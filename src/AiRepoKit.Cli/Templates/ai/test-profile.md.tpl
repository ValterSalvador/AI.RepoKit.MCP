# Test Profile

## Strategy

- Prefer focused tests near the changed code.
- Use solution-level tests only when the change crosses boundaries.
- Record skipped tests and the reason.

## Restrictions

Do not run tests that require external services, databases, Docker, or migrations unless a human explicitly approves them.
